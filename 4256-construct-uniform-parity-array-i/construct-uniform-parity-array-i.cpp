class Solution {
public:
    bool uniformArray(vector<int>& nums1) {

        return true;
        
        // bool hasOdd = false;
        // bool hasEven = false;

        // for (int x : nums1) {
        //     if (x % 2 == 0)
        //         hasEven = true;
        //     else
        //         hasOdd = true;
        // }

        // // If all numbers already have the same parity
        // if (!hasOdd || !hasEven)
        //     return true;

        // // If both odd and even exist,
        // // subtract an odd number from every even number.
        // return true;
    }
};